# feature:profile:api

Navigation keys for the profile feature. No business logic here.

## Nav Keys

| Key | Description |
|-----|-------------|
| `ProfileNavKey` | Entry point to the user profile screen. |

## Package

`com.momosi.trucktrack.feature.profile.api`

## Depends On

`:core:navigation` only (via `trucktrack.feature.api` convention plugin).

