# feature:sign-in:api

Navigation keys for the sign-in feature. No business logic here.

## Nav Keys

| Key | Description |
|-----|-------------|
| `SignInNavKey` | Entry point to the sign-in screen. Used as the initial backstack key when unauthenticated. |

## Package

`com.momosi.trucktrack.feature.signin.api`

## Depends On

`:core:navigation` only (via `trucktrack.feature.api` convention plugin).

